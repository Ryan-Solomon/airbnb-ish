import React, { FC } from 'react';
import styled from 'styled-components/native';
import { SText } from '../styles/text';

type TProps = {
  //
};

export const Post: FC<TProps> = ({}) => {
  return <SText fontColor='black'>Post</SText>;
};
